#pragma once

/** @brief Interactive terminal UI for stepping through and inspecting a running processor::CPU. */
namespace visualiser {
  /** @brief Build the tabbed UI and run the interactive full-screen event loop until the user quits. */
  void launch();
}
